version: 2

sources:
  - name:  dbt_RAW_jaffle_shop
    database: test_db
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
              
      - name: orders
        columns:
          - name: id
            tests:
              - unique
              - not_null

        loaded_at_field: _etl_loaded_at
        freshness: 
          warn_after:
            count: 1
            period: day
          error_after:
            count: 2
            period: day
        
